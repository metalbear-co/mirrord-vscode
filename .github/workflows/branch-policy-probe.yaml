name: Branch policy probe
on:
  pull_request:
    paths:
      - .github/workflows/branch-policy-probe.yaml

jobs:
  probe:
    runs-on: ubuntu-24.04
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - run: echo "ref=$GITHUB_REF"
